---
interface Item {
  label: string;
  href: string;
}

interface Props {
  label?: string;
  items: Item[];
}

const { label = "Page sections", items } = Astro.props;
---

<nav class="showcase-anchor-nav" aria-label={label}>
  <ul>
    {items.map((item) => (
      <li data-showcase-anchor-item><a href={item.href} data-showcase-view-link>{item.label}</a></li>
    ))}
  </ul>
</nav>
