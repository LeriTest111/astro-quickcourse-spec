---
import type { HeroAction } from "./hero-types";

interface Props {
  actions: HeroAction[];
  tone?: "default" | "inverse";
}

const { actions, tone = "default" } = Astro.props;
---

{
  actions.length > 0 && (
    <div class:list={["hero-actions", tone === "inverse" && "hero-actions--inverse"]} data-hero-actions>
      {actions.map((action) => (
        <a
          class:list={[
            "hero-action",
            action.variant === "secondary" && "hero-action--secondary",
          ]}
          data-hero-part={action.variant === "secondary" ? "secondary-action" : "primary-action"}
          href={action.href}
        >
          {action.label}
        </a>
      ))}
    </div>
  )
}
